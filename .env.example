# Copy to .env and set values. Do not commit .env.

# Required for adaptive question generation (OpenAI). Get key from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-...

# Optional: AWS region (default us-east-1)
# AWS_REGION=us-east-1

# Optional: adaptive model (default gpt-4o-mini)
# OPENAI_MODEL_ADAPTIVE=gpt-4o-mini
